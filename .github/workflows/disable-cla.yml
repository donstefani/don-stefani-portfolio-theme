name: Disable CLA Check
# This workflow attempts to override automatic CLA checks from GitHub Apps
on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  disable-cla:
    runs-on: ubuntu-latest
    steps:
      - name: Skip CLA Check
        run: echo "CLA check disabled for this repository"
